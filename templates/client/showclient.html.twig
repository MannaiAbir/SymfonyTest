
<table border="3">

    <tr>
        <td>Id</td>
        <td>Name</td>
        <td>Validation</td>
        <td>ACtion</td>
        
    </tr>
    {% for c in clienttab %}
    <tr>
        <td>{{c.id}}</td>
        <td>{{c.name}}</td>
        <td>{{ c.validztion ? 'Yes' : 'No' }}</td>
        <td><a href="{{path('app_deleteclient', { id: c.id })}}">delete</td>
     </tr>
        
    {% endfor %}
    </table>
    <a href="{{path('app_addclient')}}">Add</td>